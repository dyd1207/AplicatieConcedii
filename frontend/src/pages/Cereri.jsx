import PageContainer from "../components/layout/PageContainer";

export default function Cereri() {
  return (
    <PageContainer title="Cereri">
      <div className="app-card p-3">Urmează: listă cereri + formular cerere (cardbox).</div>
    </PageContainer>
  );
}